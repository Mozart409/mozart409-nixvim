pre-commit:
  parallel: true
  jobs:
    - name: keep-sorted
      run: keep-sorted {staged_files}
      stage_fixed: true
commit-msg:
  jobs:
    - name: cog verify
      run: cog verify --file {1} --ignore-merge-commits
